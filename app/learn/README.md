This application primarily shows some static content that serve as a basis to use other apps in this suit.

Some of these contents are also sent to the LLM (AI) using the CAG (Cache-Augmented Generation) method but that implementation detail is irrelevant from your perspective as a user.